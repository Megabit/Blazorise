@typeparam TValue
@inherits Blazorise.RadioGroup<TValue>
<CascadingValue Value=this>
    @if ( Buttons )
    {
        <div id="@ElementId" class="@ClassNames" style="@StyleNames" @attributes="@Attributes" data-toggle="buttons">
            @ChildContent
        </div>
        @Feedback
    }
    else
    {
        <div id="@ElementId" class="@ClassNames" style="@StyleNames" @attributes="@Attributes">
            @ChildContent
        </div>
        @Feedback
    }
</CascadingValue>