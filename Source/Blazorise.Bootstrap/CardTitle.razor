@inherits Blazorise.CardTitle
@if ( Size != null )
{
    <Dynamic TagName="@($"h{Size}")" id="@ElementId" class="@ClassNames" style="@StyleNames" @attributes="@Attributes">
        @ChildContent
    </Dynamic>
}
else
{
    <Dynamic TagName="@(InsideHeader ? "span" : "div")" id="@ElementId" class="@ClassNames" style="@StyleNames" @attributes="@Attributes">
        @ChildContent
    </Dynamic>
}