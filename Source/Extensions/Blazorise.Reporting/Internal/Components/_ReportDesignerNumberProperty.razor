@namespace Blazorise.Reporting.Internal
@inherits BaseReportComponent
@typeparam TValue

<Field Class="b-report-designer-number-property" Horizontal Margin="MarginIs0FromBottomIs0OnX">
    <FieldLabel ColumnSize="ColumnSizeIs4" TextSize="TextSizeSmall" Padding="PaddingIs1FromEnd" TextOverflow="TextOverflowTruncate">@Label</FieldLabel>
    <FieldBody ColumnSize="ColumnSizeIs8">
        @if ( Max.HasValue )
        {
            <NumericInput TValue="TValue" Value="@Value" ValueChanged="@Changed" Immediate="true" Min="@ConvertValue( Min )" Max="@ConvertValue( Max.Value )" Step="@Step" Size="Size.Small" />
        }
        else
        {
            <NumericInput TValue="TValue" Value="@Value" ValueChanged="@Changed" Immediate="true" Min="@ConvertValue( Min )" Step="@Step" Size="Size.Small" />
        }
    </FieldBody>
</Field>