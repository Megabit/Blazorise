@typeparam TNode
@inherits BaseComponent
<div class="b-tree-view-node-title">
    @if ( ParentTreeViewState.SelectionMode == TreeViewSelectionMode.Multiple )
    {
        <div class="b-tree-view-node-check">
            <Check TValue="bool" Value="@Checked" ValueChanged="@OnCheckedChanged" Disabled="@NodeState.Disabled" />
        </div>
    }
    <span class="@ClassNames" style="@(GetCurrentStyle())" @onclick="@OnClick">
        @ChildContent
    </span>
</div>