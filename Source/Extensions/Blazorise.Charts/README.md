## Things to be aware of

If you are updating any of the chart packages, please make sure to also update the following files accordingly:

- `chartjs-plugin-streaming`: Search `._chart.getDatasetMeta`, and replace with `.chart.getDatasetMeta`