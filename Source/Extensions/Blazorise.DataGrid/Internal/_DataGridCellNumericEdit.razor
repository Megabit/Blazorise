@using Blazorise.Utilities
@typeparam TItem
@inherits ComponentBase

@if ( valueType.IsNumeric() )
{
    if ( Column.NativeInputMode )
    {
        @NumericInputFragment
    }
    else
    {
        @NumericPickerFragment
    }
}