namespace Blazorise.Maps;

/// <summary>
/// Defines the map provider used to render maps.
/// </summary>
public enum MapProvider
{
    /// <summary>
    /// Uses Leaflet as the map provider.
    /// </summary>
    Leaflet = 0,
}