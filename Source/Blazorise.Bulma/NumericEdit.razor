@typeparam TValue
@inherits BaseNumericEdit<TValue>
@if ( ParentIsFieldBody )
{
    <div class="field">
        <div class="control">
            <input @ref="ElementRef" id="@ElementId" inputmode="numeric" class="@Classnames" style="@Stylenames" placeholder="@Placeholder" disabled="@IsDisabled" readonly="@IsReadonly" step="@Step" pattern="@Pattern" value="@Value" @onchange="@HandleOnChange" @oninput="@HandleOnInput" />
            @ChildContent
        </div>
        @Feedback
    </div>
}
else
{
    <div class="control">
        <input @ref="ElementRef" id="@ElementId" inputmode="numeric" class="@Classnames" style="@Stylenames" placeholder="@Placeholder" disabled="@IsDisabled" readonly="@IsReadonly" step="@Step" pattern="@Pattern" value="@Value" @onchange="@HandleOnChange" @oninput="@HandleOnInput" />
        @ChildContent
    </div>
    @Feedback
}