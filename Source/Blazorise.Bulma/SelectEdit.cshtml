@inherits Base.BaseSelect
@if ( ParentIsFieldBody )
{
    <div class="field">
        <div class="control">
            <div class="@ClassMapper.Class">
                <select id="@ElementId" disabled="@IsDisabled" readonly="@IsReadonly" multiple="@IsMultiple" onchange="@SelectionChangedHandler">
                    <CascadingValue Value=this>
                        @ChildContent
                    </CascadingValue>
                </select>
            </div>
        </div>
    </div>
}
else
{
    <div class="control">
        <div class="@ClassMapper.Class">
            <select id="@ElementId" disabled="@IsDisabled" readonly="@IsReadonly" multiple="@IsMultiple" onchange="@SelectionChangedHandler">
                <CascadingValue Value=this>
                    @ChildContent
                </CascadingValue>
            </select>
        </div>
    </div>
}