@typeparam TValue
@inherits BaseSelect<TValue>
@if ( ParentIsFieldBody )
{
    <div class="field">
        <div class="control">
            <div class="@ClassMapper.Class" style="@StyleMapper.Style">
                <select @ref="@ElementRef" id="@ElementId" disabled="@IsDisabled" readonly="@IsReadonly" multiple="@IsMultiple" @onchange="@HandleOnChanged">
                    <CascadingValue Value=this>
                        @ChildContent
                    </CascadingValue>
                </select>
            </div>
        </div>
        @Feedback
    </div>
}
else
{
    <div class="control">
        <div class="@ClassMapper.Class" style="@StyleMapper.Style">
            <select @ref="@ElementRef" id="@ElementId" disabled="@IsDisabled" readonly="@IsReadonly" multiple="@IsMultiple" @onchange="@HandleOnChanged">
                <CascadingValue Value=this>
                    @ChildContent
                </CascadingValue>
            </select>
        </div>
    </div>
    @Feedback
}