@inherits Blazorise.Tab
<button @ref="@ElementRef" id="@ElementId" role="tab" aria-labelledby="@AriaLabelledBy" tabindex="@TabIndex" type="button" aria-controls="@AriaControls" aria-selected="@AriaSelected" value="@Name" class="@ClassNames" style="@StyleNames" disabled="@DisabledString" @onclick="@ClickHandler" @attributes="@Attributes">
    <span class="@LinkClassNames" style="@Styles?.Link">
        @ChildContent
    </span>
    @if ( !Active )
    {
        <span class="fui-Tab__content--reserved-space">
            @ChildContent
        </span>
    }
</button>
@code {
    protected override string TabElementId => ElementId;

    string AriaSelected => Active ? "true" : "false";

    string DisabledString => Disabled ? "true" : null;
}