@inherits Blazorise.Base.BaseModalBackdrop
@if ( !ComponentMapper.HasRegistration( this ) )
{
    <div id="@ElementId" class="@ClassMapper.Class">
        @ChildContent
    </div>
}
else
{
    @RenderCustomComponent()
}