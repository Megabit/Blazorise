@inherits Blazorise.BaseBreadcrumbItem
@if ( !HasCustomRegistration )
{
    <CascadingValue Value=this>
        <li class="@Classnames" aria-current="@(IsActive ? "page" : null)">
            @ChildContent
        </li>
    </CascadingValue>
}
else
{
    @RenderCustomComponent()
}