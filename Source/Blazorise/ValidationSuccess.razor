@inherits Blazorise.Base.BaseValidationSuccess
@if ( !ComponentMapper.HasRegistration( this ) )
{
    @if ( ParentValidation?.Status == ValidationStatus.Success )
    {
        <div class="@ClassMapper.Class">
            @ChildContent
        </div>
    }
}
else
{
    @RenderCustomComponent()
}