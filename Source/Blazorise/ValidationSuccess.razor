@inherits Blazorise.BaseValidationSuccess
@if ( !HasCustomRegistration )
{
    @if ( ParentValidation?.Status == ValidationStatus.Success )
    {
        <div class="@ClassNames" @attributes="@Attributes">
            @ChildContent
        </div>
    }
}
else
{
    @RenderCustomComponent()
}