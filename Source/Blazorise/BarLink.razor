@inherits Blazorise.BaseBarLink
@if ( !HasCustomRegistration )
{
    @if ( To != null )
    {
        <LinkBase TagName="a" class="@Classnames" href="@To" title="@Title" Match="@Match">
            @ChildContent
        </LinkBase>
    }
    else
    {
        <a class="@Classnames" title="@Title" @onclick="@ClickHandler">
            @ChildContent
        </a>
    }
}
else
{
    @RenderCustomComponent()
}