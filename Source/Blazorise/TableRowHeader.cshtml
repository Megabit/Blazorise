@inherits Blazorise.Base.BaseTableRowHeader
@if ( !ComponentMapper.HasRegistration( this ) )
{
    <th scope="row" class="@ClassMapper.Class">
        @ChildContent
    </th>
}
else
{
    @RenderCustomComponent()
}