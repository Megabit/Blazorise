@inherits Blazorise.Base.BaseFieldHelp
@if ( !ComponentMapper.HasRegistration( this ) )
{
    <small class="@ClassMapper.Class">
        @ChildContent
    </small>
}
else
{
    @RenderCustomComponent()
}