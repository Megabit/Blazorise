namespace Blazorise;

/// <summary>
/// Defines where utility classes and styles are applied.
/// </summary>
public enum UtilityTarget
{
    /// <summary>
    /// Apply utilities to the component element itself.
    /// </summary>
    Self,

    /// <summary>
    /// Apply utilities to a wrapper element around the component.
    /// </summary>
    Wrapper,
}