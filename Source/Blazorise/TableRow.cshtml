@inherits Blazorise.Base.BaseTableRow
@if ( !ComponentMapper.HasRegistration( this ) )
{
    <tr class="@ClassMapper.Class" onclick="@HandleClick">
        @ChildContent
    </tr>
}
else
{
    @RenderCustomComponent()
}