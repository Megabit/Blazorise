@inherits Blazorise.BaseAlert
@if ( !HasCustomRegistration )
{
    @if ( ( IsDismisable == false && IsShow ) || IsDismisable )
    {
        <div class="@ClassNames" role="alert">
            @ChildContent
        </div>
    }
}
else
{
    @RenderCustomComponent()
}
