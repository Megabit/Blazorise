@inherits Blazorise.Base.BaseTableBody
@if ( !ComponentMapper.HasRegistration( this ) )
{
    <tbody class="@ClassMapper.Class">
        @ChildContent
    </tbody>
}
else
{
    @RenderCustomComponent()
}