@inherits Blazorise.Base.BaseMemo
@if ( !ComponentMapper.HasRegistration( this ) )
{
    <textarea class="@ClassMapper.Class" placeholder="@Placeholder" disabled="@IsDisabled" readonly="@IsReadonly" value="@Text" maxlength="@MaxLength" rows="@Rows" onchange="@HandleOnChange" oninput="@HandleOnInput" />
    @ChildContent
}
else
{
    @RenderCustomComponent()
}