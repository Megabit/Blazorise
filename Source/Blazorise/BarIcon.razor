@inherits BaseComponent
@if (!HasCustomRegistration)
{
    @if (IconName is RenderFragment)
    {
        @( IconName as RenderFragment )
    }
    else
    {
        <Icon Name="@IconName" Class="b-bar-icon" />
    }
}
else
{
    @RenderCustomComponent()
}