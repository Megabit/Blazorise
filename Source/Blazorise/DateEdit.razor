@inherits Blazorise.BaseDateEdit
@if ( !HasCustomRegistration )
{
    <input @ref="@ElementRef" id="@ElementId" type="date" class="@Classnames" style="@StyleMapper.Style" disabled="@IsDisabled" readonly="@IsReadonly" pattern="@Pattern" value="@internalDate" @onchange="@InternalDateHandler" @onclick="@ClickHandler" placeholder="@Placeholder" min="@Min?.ToString(Utils.Parsers.InternalDateFormat)" max="@Max?.ToString(Utils.Parsers.InternalDateFormat)" />
    @ChildContent
    @Feedback
}
else
{
    @RenderCustomComponent()
}