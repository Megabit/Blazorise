@inherits BaseComponent
@if ( !HasCustomRegistration )
{
    @if ( Responsive )
    {
        <div class="@ResponsiveClassNames">
            <table @ref="@ElementRef" class="@ClassNames" style="@StyleNames" @attributes="@Attributes">
                @ChildContent
            </table>
        </div>
    }
    else
    {
        <table @ref="@ElementRef" class="@ClassNames" style="@StyleNames" @attributes="@Attributes">
            @ChildContent
        </table>
    }
}
else
{
    @RenderCustomComponent()
}