@inherits BaseCheckEdit
@if ( !HasCustomRegistration )
{
    <input @ref="@ElementRef" id="@ElementId" type="@Type" class="@ClassNames" style="@StyleNames" disabled="@IsDisabled" readonly="@IsReadonly" checked="@Checked" @onchange="@HandleCheckedChanged" name="@RadioGroup" />
    @ChildContent
    @Feedback
}
else
{
    @RenderCustomComponent()
}