@inherits Blazorise.BaseProgressBar
@if ( !HasCustomRegistration )
{
    <div class="@ClassNames" style="@StyleNames" role="progressbar" aria-valuenow="@Value " aria-valuemin="@Min" aria-valuemax="@Max">
        @ChildContent
    </div>
}
else
{
    @RenderCustomComponent()
}