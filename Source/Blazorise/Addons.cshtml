@inherits Blazorise.Base.BaseAddons
@if ( !ComponentMapper.HasRegistration( this ) )
{
    <CascadingValue Value=this>
        <div class="@ClassMapper.Class">
            @ChildContent
        </div>
    </CascadingValue>
}
else
{
    @RenderCustomComponent()
}
