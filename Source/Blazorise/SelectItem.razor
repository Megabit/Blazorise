@typeparam TValue
@inherits Blazorise.Base.BaseSelectItem<TValue>
@if ( !ComponentMapper.HasRegistration( this ) )
{
    <option class="@ClassMapper.Class" value="@Value" selected="@IsSelected" disabled="@IsDisabled">@ChildContent</option>
}
else
{
    @RenderCustomComponent()
}