<Div Class="b-blog-post-info" Flex="Flex.AlignItems.Center" Margin="Margin.Is3.OnY">
    <Div Margin="Margin.Is2.FromEnd">
        @{
            // If the ImageName already looks like a URL, use it as-is
            var avatarSrc = string.IsNullOrWhiteSpace( ImageName )
            ? "img/avatars/default.png"
            : ( ImageName.StartsWith( "http", StringComparison.OrdinalIgnoreCase ) || ImageName.StartsWith( "/" ) )
            ? ImageName
            : $"img/avatars/{ImageName}.png";
        }
        <Image Source="@avatarSrc" Text="Blog creator avatar image" Border="Border.Rounded" Loading Class="img-circle" Style="width:48px;height:48px;" />
    </Div>
    <Div>
        <Div Class="b-blog-post-info-user">
            <Span>@UserName</Span>
        </Div>
        <Div Class="b-blog-post-info-date">
            <Span>@PostedOn</Span>
            <Span>·</Span>
            <Span>@Read</Span>
        </Div>
    </Div>
</Div>
@code {
    [Parameter] public string ImageName { get; set; }

    [Parameter] public string UserName { get; set; }

    [Parameter] public string PostedOn { get; set; }

    [Parameter] public string Read { get; set; }
}