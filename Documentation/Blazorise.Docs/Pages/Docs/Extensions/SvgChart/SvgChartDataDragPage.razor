@page "/docs/extensions/svg-chart-data-drag"

<Seo Canonical="/docs/extensions/svg-chart-data-drag" Title="Blazorise SVG Chart Data Point Dragging" Description="Learn how to edit native SVG chart values by dragging data points with pointer or keyboard input." />

<DocsPageTitle Path="Extensions/SVG Chart Data Point Dragging">
    Blazorise SVG Chart Data Point Dragging
</DocsPageTitle>

<DocsPageLead>
    Let users edit values directly in every native SVG chart type.
</DocsPageLead>

<DocsPageParagraph>
    Add <Code>SvgChartDataDrag</Code> to enable dragging declaratively, or configure <Code>SvgChartOptions.DataDrag</Code>. Line, area, and column series support vertical <Code>Y</Code> dragging. Bar series support horizontal <Code>X</Code> dragging. Scatter and bubble series support <Code>X</Code>, <Code>Y</Code>, or <Code>XY</Code> dragging. Radar and polar area values are changed by dragging radially, while pie and doughnut values are changed by dragging around the chart. Stacked area, bar, and column series update the selected segment value and recalculate the stack. Set <Code>Draggable</Code> to <Code>false</Code> on an individual series to exclude it.
</DocsPageParagraph>

<DocsPageParagraph>
    Use <Code>XStep</Code> and <Code>YStep</Code> to snap values, <Code>CanDrag</Code> to filter individual points, and <Code>DragStarted</Code>, <Code>Dragging</Code>, and <Code>DragEnded</Code> to observe changes. Persist the final value to your data source in <Code>DragEnded</Code>. Tooltips are suppressed while dragging by default; set <Code>ShowTooltip</Code> to <Code>true</Code> to display a live value tooltip. Press an arrow key while a point is focused to adjust it, hold Shift for a larger step, or press Escape during pointer dragging to restore the original value.
</DocsPageParagraph>

<DocsPageSectionTitle>
    Examples
</DocsPageSectionTitle>

<DocsPageSection>
    <DocsPageSectionHeader Title="Editable forecast">
        Drag a forecast marker vertically. Values snap to increments of five, while the baseline series remains read-only.
    </DocsPageSectionHeader>
    <DocsPageSectionContent Code="SvgChartDataDragExample" Outlined FullWidth>
        <SvgChartDataDragExample />
    </DocsPageSectionContent>
</DocsPageSection>

<DocsPageSectionTitle>
    Limitations
</DocsPageSectionTitle>

<DocsPageParagraph>
    Data point dragging is not enabled while chart streaming is active. Category positions on line, area, bar, column, radar, and polar area charts remain fixed; use scatter or bubble data when the category position must also be editable. Bubble radius is not changed by data dragging.
</DocsPageParagraph>

<DocsPageApi>
    <DocsPageApiItem Url="docs/extensions/svg-chart/api" Name="<SvgChartDataDrag />" />
    <DocsPageApiItem Url="docs/extensions/svg-chart/api" Name="SvgChartDataDragOptions" />
    <DocsPageApiItem Url="docs/extensions/svg-chart/api" Name="SvgChartDataPointDragEventArgs" />
    <DocsPageApiItem Url="docs/extensions/svg-chart/api" Name="SvgChartDataDragMode" />
</DocsPageApi>