@if (Loaded)
{
    <div Wapper>
        <ChildContent />
        @if (Busy)
        {
            <div Indicator>
                <IndicatorTemplate />
            </div Indicator>
        }
    </div Wrapper>
}
else
{
    <LoadingTemplate />
}